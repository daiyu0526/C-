#include<stdio.h>

int main(void)
{
	double sum = 0.0;
	int i,number;

	scanf_s("%d", &number);
	for (i = 1; i <= number; i++)
	{
		sum = sum + 1.0 / i;
		i *= (-1);
	}
	printf("sum=%f\n", sum);
	return 0;
}
